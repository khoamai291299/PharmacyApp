<Window x:Class="PharmacyApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Đăng nhập"
        Height="550"
        Width="550"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#E6F0FA">

    <Grid>
        <!-- Background hình minh họa nhà thuốc -->
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/PharmacyApp;component/Assets/Images/pharmacity-bg.png"
                        Stretch="UniformToFill"
                        Opacity="0.15"/>

        </Grid.Background>

        <Border CornerRadius="15" Background="White" Padding="30" Margin="50" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" Width="350">
                <TextBlock Text="ĐĂNG NHẬP"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Tên đăng nhập" Margin="0,25,0,5" FontSize="16"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="14"
                         Height="40"
                         Padding="10"
                         materialDesign:HintAssist.Hint="Username"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>

                <TextBlock Text="Mật khẩu" Margin="0,15,0,5" FontSize="16"/>
                <PasswordBox x:Name="pwd"
                             FontSize="14"
                             Height="40"
                             Padding="10"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             PasswordChanged="PasswordBox_OnChanged"/>

                <Button Content="Đăng nhập"
                        Margin="0,25,0,0"
                        Height="45"
                        Background="#3498DB"
                        Foreground="White"
                        FontSize="18"
                        FontWeight="Bold"
                        Command="{Binding LoginCommand}"
                        Cursor="Hand"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>

                <!-- Optional: thông báo lỗi -->
                <TextBlock Text="{Binding LoginMessage}" Foreground="Red" FontSize="14" Margin="0,10,0,0" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
